import React from "react";

const SectionHeadline = ({title}) => {
  return (
    <div className="text-center p-10 m-auto">
      <h2 className="text-starship font-extrabold uppercase text-4xl">{title}</h2>
    </div>
  );
};

export default SectionHeadline;
